server {
    listen       80;
    listen  [::]:80;
    server_name  _;
    root   /usr/share/nginx/html;
    index  index.html;

    # gzip the big text assets (the ~2.3 MB guide and the CSVs compress well)
    gzip              on;
    gzip_comp_level   6;
    gzip_min_length   1024;
    gzip_types        text/html text/plain text/css text/csv application/csv
                      application/javascript application/json;

    location / {
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=3600";
    }

    location = /healthz {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }
}
